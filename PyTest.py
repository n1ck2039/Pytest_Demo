def capital_case(x):
    return x.capitalize()

def test_case1():
    assert capital_case('semaphore') == 'Semaphore'

def test_case2():
    assert capital_case('semaphore') == 'Semaphore'

def test_case3():
    assert capital_case('semaphore') == 'Semaphore'

def test_case4():
    assert capital_case('semaphore') == 'Semaphore'

def test_case5():
    assert capital_case('semaphore') == 'Semaphore'

def test_case6():
    assert capital_case('semaphore') == 'Semaphore'

def test_case7():
    assert capital_case('semaphore') == 'Semaphore'

def test_case8():
    assert capital_case('semaphore') == 'Semaphore'

def test_case9():
    assert capital_case('semaphore') == 'Semaphore'

def test_case10():
    assert capital_case('semaphore') == 'Semaphore'

def test_case11():
    assert capital_case('semaphore') == 'Semaphore'

def test_case12():
    assert capital_case('semaphore') == 'Semaphore'
