def capital_case(x):
    return x.capitalize()

def Create_Job():
    assert capital_case('semaphore') == 'Semaphore'

def Create_Sub_Job():
    assert capital_case('semaphore') == 'Semaphore'

def Update_Job():
    assert capital_case('semaphore') == 'Semaphore'

def test_case4():
    assert capital_case('semaphore') == 'Semaphore'

def test_case5():
    assert capital_case('semaphore') == 'Semaphore'

def test_case6():
    assert capital_case('semaphore') == 'Semaphore'

def test_case7():
    assert capital_case('semaphore') == 'Semaphore'

def test_case8():
    assert capital_case('semaphore') == 'Semaphore'

def test_case9():
    assert capital_case('semaphore') == 'Semaphore'

def test_case10():
    assert capital_case('semaphore') == 'Semaphore'

def test_case11():
    assert capital_case('semaphore') == 'Semaphore'

def test_case12():
    assert capital_case('semaphore') == 'Semaphore'
